# The public-facing URL of your deployment (no trailing slash)
# Used to generate QR codes and scan links — set this to your domain in production
PUBLIC_URL=https://yourdomain.com

# A strong secret key for signing JWT authentication tokens
# Generate a secure value with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SECRET_KEY=your_jwt_secret_key

# The port the server listens on (default: 3000)
PORT=3000
